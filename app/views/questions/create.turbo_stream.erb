<%# Leaving this empty just know that we can create turbo_stream.rb with multiple turbo_stream actions if we are not using turbo_stream_from and callbacks %>

<%# turbo_stream.replace "count", partial: "questions/count", locals: { list: @evaluation.questions } %>
<%# turbo_stream.append "questions", partial: "questions/question", locals: { question: @question } %>
